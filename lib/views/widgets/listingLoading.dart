import 'package:flutter/material.dart';

class ListingLoading extends StatelessWidget {
  const ListingLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Listing Loading'),
    );
  }
}
